@model IEnumerable<MVC_Tienda.USUARIO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.US_USUARIO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.US_PASS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.US_ROL)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.US_USUARIO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.US_PASS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.US_ROL)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.US_COD }) |
            @Html.ActionLink("Details", "Details", new { id=item.US_COD }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.US_COD })
        </td>
    </tr>
}

</table>
